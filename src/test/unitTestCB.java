import org.junit.Test;
import static org.junit.Assert.assertEquals;
public class unitTestCB {
   @Test
   public void testAdd() {
      String str= "Junit is working fine Prasad";
      assertEquals("Junit is working fine",str);
   }
} 